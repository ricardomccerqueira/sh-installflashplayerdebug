sh-installflashplayerdebug
==========================

shell script to install flash player debug version on mac osx mavericks ( should work on some older versions aswell )

it will create a shortcut on the commandline: $flashlog
